(ns {{name}}.core-test
  (:require
    [clojure.test :refer [deftest is testing]]
    [bract.core.dev-init]))


;; initialized context is in bract.core.dev/app-context


(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
